- Mirar const en funciones
- Broadcast implementado mirar las ops tienen que usar broadcas_shape y tambien en tensor_add y el resto donde solo hay dos parametros y el out se crea en la funcion, tambien operadores para los element wise tensors que llamen a estas ultimas funciones
